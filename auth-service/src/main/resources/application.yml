server:
  port: 8084

spring:
  application:
    name: auth-service   # 👈 lowercase

eureka:
  client:
    service-url:
      defaultZone: http://eureka-server:8761/eureka/
    register-with-eureka: true
    fetch-registry: true
  instance:
    prefer-ip-address: true
    lease-renewal-interval-in-seconds: 10
    lease-expiration-duration-in-seconds: 30

management:
  endpoints:
    web:
      exposure:
        include: health,info

jwt:
  secret: Ws1KXK81j83IDS/tqc7jS0Y3NDKHKOkc9CIEXEy1wAs=
  expiration: 3600000
