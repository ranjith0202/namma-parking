server:
  port: 8080

spring:
  application:
    name: API-GATEWAY

  cloud:
    discovery:
      reactive:
        enabled: true

    gateway:
      routes:
        - id: user-service
          uri: lb://USER-SERVICE
          predicates:
            - Path=/api/users/**
        
        - id: auth-service
          uri: lb://AUTH-SERVICE
          predicates:
            - Path=/auth/**

eureka:
  client:
    service-url:
      defaultZone: http://eureka-server:8761/eureka/
jwt:
  secret: Ws1KXK81j83IDS/tqc7jS0Y3NDKHKOkc9CIEXEy1wAs=
  expiration: 3600000
